- name: first practise example
  hosts: localhost
  vars: 
    - VAR1: Pranay
    - VAR2: Radha
  tasks:
    - name: Print hello world
      ansible.builtin.debug:
        msg1: Hello world to all
        msg2: Hi {{VAR1}} and {{VAR2}}


- name: first practise example
  hosts: localhost
  vars_files: 
    - variable.yml
  tasks:
    - name: Print hello world
      ansible.builtin.debug:
        msg3: My Favorite food is {{Favorite_food}}
        msg4: My Hobbies are {{Hobbies}}

